{
  "name": "sons-of-the-forest",
  "version": "1.0.0",
  "description": "Sons of the Forest Horror Website with JSON Server",
  "main": "index.html",
  "scripts": {
    "server": "json-server --watch db.json --port 3000",
    "start": "json-server --watch db.json --port 3000"
  },
  "keywords": [
    "sons of the forest",
    "horror",
    "game"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
